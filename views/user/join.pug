doctype html

head
	include head

body
	header
		include header

	main
		h1 Welcome to Mine Eclipse!
		//iframe(width="560" height="315" src="https://www.youtube.com/embed/NqrKGvQT0eU" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen)
		p Video will be here
		h2 Join us today by filling the form below!
		div#content-container
			form(action="/join/" method="post" enctype="multipart/form-data")
				label.required(for="firstname") First name
				input#firstname(type="text" name="firstName" placeholder="John" required)
				label.required(for="lastname") Last name
				input#lastname(type="text" name="lastName" placeholder="Doe" required)
				label.required(for="email") Your Metropolia email
				input#email(type="email" name="email" value=email disabled required)
				label.required(for="backup-email") Secondary email
				input#backup-email(type="email" name="backupEmail" placeholder="john.doe@gmail.com" required)
				label.required(for="phone") Main phone number
				input#phone(type="tel" name="phone"
					title="Enter your phone starting with 0 or +358"
					placeholder="+358400000000" pattern="(0|\\+358).{9}" required)
				label(for="phone") Secondary phone number
				input#backup-phone(type="tel" name="backupPhone"
					title="Enter your phone starting with 0 or +358"
					placeholder="+358400000000" pattern="(0|\\+358).{9}")
				label.required Select your team
				input#team-e(type="radio" value="Embedded" name="team" required)
				label(for="team-e") Embedded team
				br
				input#team-f(type="radio" value="Frontend" name="team")
				label(for="team-f") Frontend team
				br
				input#team-b(type="radio" value="Backend" name="team")
				label(for="team-b") Backend team
				br
				input#team-a(type="radio" value="Android" name="team" disabled=mobile_disabled)
				label(for="team-b") Android team
				if mobile_disabled
					span.disabled **
				br
				input#team-i(type="radio" value="iOS" name="team" disabled=mobile_disabled)
				label(for="team-b") iOS team
				if mobile_disabled
					span.disabled **
					br
					span.disabled ** Mobile teams are currently full, please check back later
				br
				label(for="links") Add useful links (GitHub, LinkedIn, etc.)
				textarea#links(name="links" placeholder="You can include links to your code, " +
				"social network pages or anything else. You can also add comments here.")
				label(for="free-form") Feel free to tell anything else you want
				textarea#free-form(name="freeForm" placeholder="Enter anything you want here. " +
				"You can tell more about yourself, your projects, achievements, work experience or anything " +
				"else you can think of.")
				label.required(for="cv") Please attach your cv
				input#cv(type="file" name="cv" required)
				input#submit(type="submit" value="Join!")
				label.label-info.required-info * Required fields

	footer
		include footer