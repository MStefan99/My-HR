doctype html

html(lang='en')
	head
		title Join us - Mine Eclipse
		include head

	body
		header
			include header

		include flash

		main
			div#splash-container
				div#splash
					span#text-left.logo-text.inactive Mine e
					img#logo.inactive(src='/img/me-logo.svg' alt='Mine Eclipse logo')
					span#text-right.logo-text.inactive lipse
				div#welcome-container.inactive
					h1 Apply now!
					h2 Start your journey with Mine Eclipse today!
					a(href='#register') Click here to get started
			div#content-container
				h1#register We're happy to see you!
				h2 Just a few steps first...
				p We need to make sure you have a Metropolia email.
					|  For this, please fill in this very simple form below and we'll get back to you in a moment!
				form(action='/register/' method='post')
					label.required(for='username') Enter your Metropolia email:
					input#username(type='text' name='username'
						placeholder='john.doe' autocomplete='username' required)
					label#metropolia-domain(for='username') @metropolia.fi
					input#submit(type='submit' value='Send email' title='Send email')
					label.label-info.required-info * Required fields
				a(href='/join') Already registered? Click here!
				p#splash-text
			script(async type='text/javascript' src='/js/user/home.js')

		footer
			include footer
