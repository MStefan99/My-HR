// Generated by CoffeeScript 2.5.1
var Storage, body;

body = document.querySelector('body');

Storage = window.localStorage;

export var saveRequest = async function(path, init) {
  if ('serviceWorker' in navigator && (await navigator.serviceWorker.getRegistration())) {
    alert('You are currently offline but we will retry this when you have connection again.');
    return indexedDB.open('my-hr').onsuccess = (function(e) {
      var db;
      db = e.target.result;
      return db.transaction('unfinishedRequests', 'readwrite').objectStore('unfinishedRequests').add({
        path: path,
        init: init
      });
    });
  } else {
    return alert('You are not currently connected to internet. Please retry when online.');
  }
};

if (Storage.getItem('mh_theme') === 'dark') {
  body.classList.add('dark-theme');
}

//# sourceMappingURL=main.js.map
