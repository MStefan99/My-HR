// Generated by CoffeeScript 2.5.1
'use strict';
var Storage;

Storage = window.localStorage;

import * as notify from '/js/console/notifications.js';

addEventListener('load', async function() {
  if (Storage.getItem('mh_about-read') !== 'true') {
    if ((await notify.ask('Welcome!', 'Hi, it\'s great to meet you! \nWelcome to My HR! \nWould you like to read a message from the developer?'))) {
      Storage.setItem('mh_about-read', 'true');
      window.location.href = '/console/about';
    } else {
      return notify.tell('Thank you', 'Thank you and have a nice day!');
    }
  }
});

addEventListener('load', async function() {
  var registration, req;
  req = indexedDB.open('my-hr', 1);
  req.onupgradeneeded = function(e) {
    var db;
    db = e.target.result;
    return db.createObjectStore('unfinishedRequests', {
      autoIncrement: true
    });
  };
  if ('serviceWorker' in navigator) {
    registration = (await navigator.serviceWorker.register('/js/console/worker.js', {
      scope: '/console/'
    }));
    await navigator.serviceWorker.ready;
    if ('sync' in registration) {
      return registration.sync.register('request-sync');
    }
  }
});

//# sourceMappingURL=home.js.map
